# Optional "HD" engine (Chatterbox Turbo). Heavy (~2-3 GB with torch). NOT in the
# default install — fetched on demand into ~/Library/Application Support/Parley/
# hd-packages when the user enables HD mode. Kept separate so the default Kokoro
# app stays light.
numpy>=1.26,<2        # chatterbox deps pin <2; kokoro-onnx runs fine on it too
torch>=2.0
torchaudio>=2.0
chatterbox-tts>=0.1.7
resemble-perth
librosa>=0.10
soundfile>=0.12
